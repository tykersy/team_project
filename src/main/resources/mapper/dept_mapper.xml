<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.project.dao.DeptDAO">
	
    <!-- 전체 부서 정보 조회 -->
	<select id = "selectAll" resultType="dept">
        select * from dept
    </select>

    <!-- 부서이름 검색 -->
    <select id="searchDept"
            parameterType="String"
            resultType="dept">
        select dname, deptno from dept
        where dname like CONCAT('%',#{search_name},'%')
        order by deptno
    </select>

    <!-- 부서1개 정보 조회 -->
    <select id="selectOne"
            parameterType="int"
            resultType="dept">
        select * from dept
        where deptno = #{deptno}
    </select>
    
    <!-- 조직도 정보 조회 -->
    <select id="getDeptOrgChart" resultType="map">
        SELECT 
            d.deptno AS deptNo,
            d.dname AS deptName,
            s.sabun AS sabun,
            s.saname AS saname,
            s.sajob AS sajob
        FROM dept d
        LEFT JOIN sawon s ON d.deptno = s.deptno AND s.sastatus = '재직'
        ORDER BY d.deptno, s.sabun
    </select>

    <!-- '조직도 라이브러리'를 위한 데이터 -->
    <select id="getOrgChartData" resultType="map">
    SELECT id, pid, name, title FROM (
        SELECT 'COMPANY' AS id, NULL AS pid, 'CEO' AS name, '본부' AS title
        UNION ALL
        SELECT CONCAT('DEPT_', deptno), 'COMPANY', dname, '부서' FROM dept
        UNION ALL
        SELECT CAST(s.sabun AS CHAR), CONCAT('DEPT_', s.deptno), s.saname, s.sajob 
        FROM sawon s WHERE s.deptno IS NOT NULL AND s.sastatus = '재직'
    ) AS org_chart
    </select>
    
    <!-- 부서별 인원 조회 -->
    <select id="memberCount"
            resultType="java.util.Map">
        select d.dname, count(s.sabun) cnt
        from dept d
        left join sawon s
        on d.deptno = s.deptno and s.sastatus = '재직'
        group by d.dname
    </select>

    <!-- 총 부서 갯수 조회 -->
    <select id="deptCnt"
            resultType="int">
        select count(deptno)cnt from dept
    </select>

    <!-- 부서별 사원 수 조회 -->
    <select id="deptCntList" resultType="map">
        SELECT
        d.dname AS deptName,
        COUNT(s.sabun) AS sawonCount
        FROM dept d
        LEFT JOIN sawon s
        ON d.deptno = s.deptno AND s.sastatus = '재직'
        GROUP BY d.deptno, d.dname
        ORDER BY sawonCount DESC
    </select>

    <!-- 부서 정보 수정 -->
    <update id="update"
            parameterType="dept">
        update dept set dname = #{dname}, dtel = #{dtel}
        WHERE deptno = #{deptno}
    </update>

    <!-- 부서명, 부서번호 중복체크 -->
    <select id="selectCheck"
            parameterType="dept"
            resultType="dept">
        select * from dept 
        where deptno = #{deptno}
        or dname = #{dname}
    </select>

    <!-- 부서등록 -->
    <insert id="insertDept"
            parameterType="dept">
        insert into dept values (#{deptno}, #{dname}, #{dtel})
    </insert>

    <!-- 부서 삭제 -->
    <delete id="deleteDept"
            parameterType="String">
        delete from dept where dname = #{dname}
    </delete>

    <!-- 조직도 상태표시 포함 -->
   <select id="chartList" resultType="orgchart">
    SELECT 
        d.deptno, d.dname, d.dtel,
        s.sabun, s.saname, s.sajob, s.saemail, s.satel,
        CASE 
            WHEN l.log_id IS NOT NULL THEN '휴가중'
            <!-- 퇴근 시간이 찍혀있다면 '퇴근' -->
            WHEN ta.checkout IS NOT NULL THEN '퇴근'
            <!-- 출근 시간만 있다면 '근무중' -->
            WHEN ta.checkin IS NOT NULL THEN '근무중'
            <!-- 위 조건에 모두 해당 안 되면 '출근전' -->
            ELSE '출근전'
        END AS status
    FROM dept d
    LEFT JOIN sawon s ON d.deptno = s.deptno AND s.sastatus = '재직'
    
    <!-- 오늘 날짜 휴가 기록 확인 -->
    LEFT JOIN sleave_log l ON s.sabun = l.sabun 
                           AND l.use_date = CURDATE() 
                           AND l.approve = 1
                           
    <!-- 오늘 날짜 근태 기록 확인 (TA 테이블의 day 기준) -->
    LEFT JOIN ta ON s.sabun = ta.sabun 
                     AND DATE(ta.day) = CURDATE() 
                     
    ORDER BY d.deptno ASC, 
             <!-- 1. 팀장류(LIKE)를 최상단(0)으로 배치 -->
             (CASE WHEN s.sajob LIKE '%팀장%' THEN 0 
             <!-- 2. 나머지 직급 우선순위 할당 -->
                   WHEN s.sajob = '부장' THEN 1
                   WHEN s.sajob = '차장' THEN 2
                   WHEN s.sajob = '과장' THEN 3
                   WHEN s.sajob = '대리' THEN 4
                   WHEN s.sajob = '주임' THEN 5
                   WHEN s.sajob = '사원' THEN 6
                   ELSE 7 END) ASC, 
            <!-- 같은 직급 내에서는 사번 순 -->
             s.sabun ASC 
    </select>

    <!-- 현재 DB중 가장 큰 부서번호 조회 -->
    <select id="getBiggestDeptno"
            resultType="int">
        select max(deptno) from dept
    </select>

</mapper>